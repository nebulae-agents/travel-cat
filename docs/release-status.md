# 候选版验收状态

此文件区分开源源码与正式二进制发行验收；开源不表示生产功能或安装验收已经完成。

## 已验证的源码能力

- 可移植的默认数据目录和签名辅助程序入口。
- 角色导入、旅程身份冻结、独立测试会话与角色素材导出备份。
- 插件在用户或系统 Applications 中查找唯一合格应用；不依赖作者路径。
- 明信片任务使用冻结角色和运行环境返回的资源位置，严格映射结果字段。
- 默认黑猫安全安装器：原始字节校验、同名冲突保护、原子不覆盖安装及临时目录所有权检查。
- 测试命令见 README 和 CI 配置；结果应以对应提交的实际运行记录为准。

部分验收需要显式会话、真实生成授权或用户原始明信片；辅助入口也可能只在子进程中执行。不能把离线回归通过理解为已验证真实生成或私有图像。

## 仍需完成的发行检查

源码已提供 `Scripts/package-project.py` 作为可重复候选组装入口；它的安全解压、布局、素材字节、权限和不覆盖边界由小型夹具测试保护。此处只说明工具可用，不代表实际候选 ZIP 已生成或下列验收已通过。

- 实际归档布局及解压后的安全安装器验证。
- 从干净源码归档独立解压、构建和测试。
- 候选包签名、原始素材校验值、体系结构和校验清单检查。
- 隔离数据的候选程序启动、设置、纸条、菜单与相册验收。
- 另一账户下实际 Codex 插件安装、宠物启用和真实生成。

现有程序采用临时签名，尚无 Developer ID 签名或 Apple 公证。代码、文档和自有项目素材使用 Apache-2.0；第三方字体保留原许可。源码发布不会替换已安装应用、注册插件市场或修改生产自动化。
