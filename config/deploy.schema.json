{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Travel Cat deployment configuration",
  "description": "Public suggestions may use ~/. Private configuration must contain expanded absolute paths; runtime validation also checks path safety and overlap.",
  "type": "object",
  "additionalProperties": false,
  "required": ["schemaVersion", "applicationDirectory", "dataRoot"],
  "properties": {
    "schemaVersion": {"type": "integer", "const": 2},
    "applicationDirectory": {"type": "string", "minLength": 1, "pattern": "^(~/|/)"},
    "dataRoot": {"type": "string", "minLength": 1, "pattern": "^(~/|/)"}
  }
}
