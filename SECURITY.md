# 安全与隐私

请勿将以下内容附在公开 issue、讨论或提交中：

- `.local/` 中的实际配置、备份、部署记录及构建日志。
- `TravelPetData`、完整旅行相册、私人叙事和角色参考图。
- 登录凭据、令牌、密钥、账户配置或未经脱敏的诊断输出。

发现安全问题时，优先使用仓库提供的私密安全报告入口（若已启用），或先通过与维护者约定的私密渠道沟通。若尚无私密渠道，公开问题中只请求建立联系，不公开漏洞利用细节、私人数据或凭据。本项目尚未承诺响应时限或受支持的安全版本范围。

如果凭据已经公开，应先在对应服务中撤销或轮换；仅删除工作区文件不能清除 Git 历史、已有副本或已上传内容。不要假定本项目上传检查可以发现所有秘密。

本地存储不表示生成过程离线：调用 Codex 生成能力时，相关叙事、参考图和上下文可能交给生成服务。测试和 CI 不应调用真实生成或消耗用户额度。

应用目前使用临时签名，并非 Apple 公证的公开发行包。不要为了安装或更新而关闭系统安全保护、移除 quarantine 或直接修改 Codex 内部程序及插件缓存。安装前核对来源、明确提交与校验结果；出现恢复失败时保留备份，停止重复部署并检查实际状态。

进一步边界见[现有安全说明](docs/security.md)、[本地部署说明](docs/local-deployment.md)和[公开源码策略](docs/public-source-policy.md)。
